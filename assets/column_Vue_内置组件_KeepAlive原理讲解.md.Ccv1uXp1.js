import{_ as n}from"./chunks/plugin-vue_export-helper.DlAUqK2U.js";import{B as i,c as t,f as l,q as a,D as o,Q as r,g as p}from"./chunks/runtime-core.esm-bundler.BPXnTuTE.js";const v=JSON.parse('{"title":"KeepAlive原理讲解","description":"","frontmatter":{},"headers":[],"relativePath":"column/Vue/内置组件/KeepAlive原理讲解.md","filePath":"column/Vue/内置组件/KeepAlive原理讲解.md","lastUpdated":1785208098000}'),d={name:"column/Vue/内置组件/KeepAlive原理讲解.md"};function h(c,s,k,g,u,E){const e=i("ArticleMetadata");return l(),t("div",null,[s[0]||(s[0]=a("h1",{id:"keepalive原理讲解",tabindex:"-1"},[p("KeepAlive原理讲解 "),a("a",{class:"header-anchor",href:"#keepalive原理讲解","aria-label":'Permalink to "KeepAlive原理讲解"'},"​")],-1)),o(e),s[1]||(s[1]=r(`<p>Vue 中的 <code>&lt;KeepAlive&gt;</code> 是一个<strong>内置抽象组件</strong>（Abstract Component），它本身不会渲染具体的 DOM 节点，也不体现在父子组件链中。它的核心功能是在组件切换时<strong>缓存组件的虚拟 DOM（VNode）及真实 DOM 节点</strong>，避免重复创建和销毁消耗性能。</p><h2 id="一、核心原理拆解" tabindex="-1">一、核心原理拆解 <a class="header-anchor" href="#一、核心原理拆解" aria-label="Permalink to &quot;一、核心原理拆解&quot;">​</a></h2><h3 id="_1-内部缓存机制与-lru-策略" tabindex="-1">1. 内部缓存机制与 LRU 策略 <a class="header-anchor" href="#_1-内部缓存机制与-lru-策略" aria-label="Permalink to &quot;1. 内部缓存机制与 LRU 策略&quot;">​</a></h3><p><code>&lt;KeepAlive&gt;</code> 内部维护了两个核心数据结构：</p><ul><li><strong><code>cache</code> 映射</strong>：存储组件标识 <code>key</code> 到组件实例 / VNode 的映射关系。</li><li><strong><code>keys</code> 集合/队列</strong>：记录 <code>key</code> 的访问顺序，用于实现 <strong>LRU（Least Recently Used，最近最少使用）</strong> 缓存淘汰算法。</li></ul><p>当设置了 <code>max</code> 属性且缓存数量达到上限时，<code>&lt;KeepAlive&gt;</code> 会删除最久未访问的组件实例，并调用其真正的销毁逻辑（<code>unmount</code>）。</p><h3 id="_2-渲染拦截与-vnode-复用" tabindex="-1">2. 渲染拦截与 VNode 复用 <a class="header-anchor" href="#_2-渲染拦截与-vnode-复用" aria-label="Permalink to &quot;2. 渲染拦截与 VNode 复用&quot;">​</a></h3><p>在 <code>&lt;KeepAlive&gt;</code> 的 <code>render</code> / <code>setup</code> 处理流程中：</p><ol><li><strong>提取子节点</strong>：获取默认插槽（<code>slots.default</code>）中的第一个子组件 VNode。</li><li><strong>规则匹配</strong>：检测组件名称是否匹配 <code>include</code> / <code>exclude</code>。若不匹配，直接返回原 VNode，不走缓存。</li><li><strong>缓存命中</strong>：</li></ol><ul><li><strong>未命中</strong>：将该 VNode / 组件实例存入 <code>cache</code>，并将其 <code>key</code> 压入 <code>keys</code> 队列尾部（表示最新使用）。</li><li><strong>已命中</strong>：直接复用已有组件实例 <code>vnode.component = cachedVNode.component</code>，并将 <code>key</code> 更新到 <code>keys</code> 队列的最新位置。</li></ul><ol start="4"><li><strong>设置标记</strong>：为 VNode 标记特殊标识（如 Vue 3 的 <code>ShapeFlags.COMPONENT_KEPT_ALIVE</code>），指示渲染器在卸载该节点时“只隐藏不销毁”。</li></ol><h3 id="_3-dom-节点的保留与挂载" tabindex="-1">3. DOM 节点的保留与挂载 <a class="header-anchor" href="#_3-dom-节点的保留与挂载" aria-label="Permalink to &quot;3. DOM 节点的保留与挂载&quot;">​</a></h3><p>在 Vue 的渲染器（Renderer）层面，<code>&lt;KeepAlive&gt;</code> 的组件挂载与卸载逻辑被重写：</p><ul><li><strong>失活（Deactivate）</strong>：当切出缓存组件时，渲染器不会销毁它，而是将其对应的真实 DOM 转移到一个<strong>在内存中挂载的隐藏容器</strong>中，同时触发组件的 <code>deactivated</code> 生命周期钩子。</li><li><strong>激活（Activate）</strong>：当再次切回该组件时，渲染器直接将内存中已有的 DOM 节点重新插入到目标父容器中，同时触发 <code>activated</code> 生命周期钩子，绕过了重新创建组件和重新渲染 DOM 的开销。</li></ul><h2 id="二、简单案例描述" tabindex="-1">二、简单案例描述 <a class="header-anchor" href="#二、简单案例描述" aria-label="Permalink to &quot;二、简单案例描述&quot;">​</a></h2><p>为了简单易懂，我把那些专业的术语（比如“VNode”、“LRU 算法”）全部抛开，用<strong>一个现实生活中的“展柜”例子</strong>来通俗解释：</p><h3 id="_1-传统组件-vs-keepalive-组件" tabindex="-1">1. 传统组件 vs <code>&lt;KeepAlive&gt;</code> 组件 <a class="header-anchor" href="#_1-传统组件-vs-keepalive-组件" aria-label="Permalink to &quot;1. 传统组件 vs \`&lt;KeepAlive&gt;\` 组件&quot;">​</a></h3><ul><li><strong>传统组件（不加 KeepAlive）</strong>： 就像<strong>现场砌砖造房子</strong>。 当你切到 Tab B 时，Tab A 这栋房子会被<strong>直接强拆</strong>（触发 <code>unmounted</code> 销毁）； 当你再切回 Tab A 时，又必须<strong>重新从打地基开始建房子</strong>（触发 <code>created</code>/<code>mounted</code>），之前里面住的人、填的数据全部清空。</li><li><strong>加了 <code>&lt;KeepAlive&gt;</code> 的组件</strong>： 就像<strong>把整个房子放进了一个“保鲜后台”</strong>。 当你切到 Tab B 时，Tab A 的房子<strong>不拆</strong>，只是搬到了一个“看不见的后台仓库”藏起来（触发 <code>deactivated</code>）； 当你切回 Tab A 时，直接把整个房子<strong>从仓库搬回前台</strong>（触发 <code>activated</code>），里面的输入框文字、滚动位置、组件状态<strong>完好无损</strong>。</li></ul><h3 id="_2-vue-在底层具体做了什么-三步走" tabindex="-1">2. Vue 在底层具体做了什么？（三步走） <a class="header-anchor" href="#_2-vue-在底层具体做了什么-三步走" aria-label="Permalink to &quot;2. Vue 在底层具体做了什么？（三步走）&quot;">​</a></h3><p>为了做到这一点，Vue 内部只做了 3 件事：</p><ol><li><strong>准备一个小本本和一个仓库</strong>：</li></ol><ul><li><strong>仓库（cache 缓存）</strong>：用来存那些不需要强拆的组件实例（以及它们的真实 DOM 节点）。</li><li><strong>小本本（keys 队列）</strong>：记录谁最近被使用过。如果规定仓库最多只能装 3 个组件，当进第 4 个时，就把最久没用过的那个<strong>彻底扔掉强拆</strong>（这就是 LRU 淘汰机制）。</li></ul><ol start="2"><li><strong>拦截“销毁”动作</strong>：</li></ol><ul><li>正常情况下，切换掉组件时 Vue 会调用 <code>unmount()</code> 销毁 DOM。</li><li>但如果外面套了 <code>&lt;KeepAlive&gt;</code>，Vue 看到标记后，就会改用 <code>deactivate()</code>：<strong>把组件的 DOM 节点从当前页面拔下来，塞进内存隐藏容器里</strong>，组件实例依然活着。</li></ul><ol start="3"><li><strong>重新切回时直接“粘贴”</strong>：</li></ol><ul><li>当你再次切换回这个组件时，Vue 先去仓库里找。</li><li>找到了！直接把刚才藏在内存里的 DOM 节点<strong>重新插入到页面上</strong>，省去了重新渲染、重新调接口、重新初始化变量的所有时间。</li></ul><h3 id="_3-一张图对比生命周期" tabindex="-1">3. 一张图对比生命周期 <a class="header-anchor" href="#_3-一张图对比生命周期" aria-label="Permalink to &quot;3. 一张图对比生命周期&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>【不加 KeepAlive 的切换流程】</span></span>
<span class="line"><span>进入组件 A ➔ [ created ] ➔ [ mounted ] (显示)</span></span>
<span class="line"><span>离开组件 A ➔ [ unmounted ] (彻底销毁，状态丢失)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>【加了 KeepAlive 的切换流程】</span></span>
<span class="line"><span>首次进入 A ➔ [ created ] ➔ [ mounted ] ➔ [ activated ] (显示)</span></span>
<span class="line"><span>离开组件 A ➔ [ deactivated ] (不销毁！藏进仓库)</span></span>
<span class="line"><span>再次切回 A ➔ [ activated ] (从仓库直接拿出来，状态完好)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>通过这种方式，<code>&lt;KeepAlive&gt;</code> 既节省了 DOM 创毁的性能消耗，又保留了用户的页面输入状态。</p><h2 id="三、-核心三大疑问深度拆解" tabindex="-1">三、 核心三大疑问深度拆解 <a class="header-anchor" href="#三、-核心三大疑问深度拆解" aria-label="Permalink to &quot;三、 核心三大疑问深度拆解&quot;">​</a></h2><h3 id="疑问-1-dom-是怎么-缓存到内存中-的" tabindex="-1">疑问 1：DOM 是怎么“缓存到内存中”的？ <a class="header-anchor" href="#疑问-1-dom-是怎么-缓存到内存中-的" aria-label="Permalink to &quot;疑问 1：DOM 是怎么“缓存到内存中”的？&quot;">​</a></h3><p>在浏览器中，<strong>DOM 节点本质上就是普通的 JavaScript 对象</strong>。</p><p>根据 JavaScript 的<strong>垃圾回收（GC）机制</strong>：只要一个对象还被某个变量引用着，垃圾回收器就不会销毁它。</p><p><code>&lt;KeepAlive&gt;</code> 内部维护了一个 <code>cache</code> 映射（Map）：</p><div class="language-javascript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. 假设这是你的组件 DOM</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> myComponentDom</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createElement</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;div&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">myComponentDom.innerHTML </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;&lt;input value=&quot;用户输入的内容&quot;&gt;&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2. 页面切走了，正常情况下会执行 removeChild 销毁：</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// parent.removeChild(myComponentDom);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 如果没有变量引用它，就被垃圾回收了</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 3. KeepAlive 的做法：把它存到一个对象（Map）里！</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cache</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cache.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;MyComponent&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, myComponentDom); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 👈 关键：建立引用！</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 4. 从页面移除，但因为它在 cache 映射里被引用着，它依然保存在内存中</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">myComponentDom.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">remove</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>在 Vue 中，<code>&lt;KeepAlive&gt;</code> 组件内部有一个 cache 对象。当组件“失活”时，Vue 只是执行了 parent.removeChild(el) 把 DOM 从页面树上拔下来，但这个 el（真实 DOM）一直被 Vue 的缓存 Map 引用着。所以它全貌（包括内部的绑定事件、状态数据）都安安静静地躺在内存里。</p><p><strong>结论</strong>：Vue 并没有什么魔术，它只是用一个 Map 变量“强行引用”了组件实例和真实 DOM 节点，阻止了浏览器的垃圾回收。</p><hr><h3 id="疑问-2-重新插入-dom-时-难道不需要渲染吗" tabindex="-1">疑问 2：重新插入 DOM 时，难道不需要渲染吗？ <a class="header-anchor" href="#疑问-2-重新插入-dom-时-难道不需要渲染吗" aria-label="Permalink to &quot;疑问 2：重新插入 DOM 时，难道不需要渲染吗？&quot;">​</a></h3><p><strong>答案是：需要渲染，但开销相差了上百倍！</strong></p><p>我们把一个组件呈现在屏幕上拆分为两个阶段：</p><ol><li><strong>【JS 计算与 DOM 创建阶段】</strong>（大头消耗）</li><li><strong>【浏览器重排重绘阶段】</strong>（渲染绘制）</li></ol><p>对比一下两者的差异：</p><table tabindex="0"><thead><tr><th>阶段 / 行为</th><th>普通切换（重新新建）</th><th>KeepAlive 切换（复用缓存）</th></tr></thead><tbody><tr><td><strong>JS 数据初始化</strong></td><td>执行 <code>setup()</code> / <code>data()</code>，重新初始化变量</td><td><strong>跳过</strong> ⚡</td></tr><tr><td><strong>网络请求</strong></td><td>重新发送 API 请求获取数据</td><td><strong>跳过</strong> ⚡</td></tr><tr><td><strong>生成 VNode</strong></td><td>重新运行 <code>render()</code>，构建整棵 VNode 树</td><td><strong>跳过</strong>（复用缓存的 VNode） ⚡</td></tr><tr><td><strong>创建 DOM</strong></td><td>递归调用 <code>document.createElement</code> 造 DOM</td><td><strong>跳过</strong> ⚡</td></tr><tr><td><strong>绑定事件</strong></td><td>重新绑定各种 <code>addEventListener</code></td><td><strong>跳过</strong> ⚡</td></tr><tr><td><strong>插入 DOM 树</strong></td><td>将新造好的 DOM 插入页面</td><td>**仅执行 <code>parent.appendChild(el)**</code></td></tr><tr><td><strong>浏览器绘制</strong></td><td>Layout &amp; Paint（重排重绘）</td><td>Layout &amp; Paint（重排重绘）</td></tr></tbody></table><p><strong>结论</strong>：<code>&lt;KeepAlive&gt;</code> 重新插入 DOM 时，彻底省去了最昂贵的 <strong>“JS 计算与 DOM 从零构建”</strong> 过程，仅保留了极轻量的“已有 DOM 节点搬家”<strong>和</strong>“浏览器绘制”。</p><hr><h3 id="疑问-3-滚动条的位置为什么能保留" tabindex="-1">疑问 3：滚动条的位置为什么能保留？ <a class="header-anchor" href="#疑问-3-滚动条的位置为什么能保留" aria-label="Permalink to &quot;疑问 3：滚动条的位置为什么能保留？&quot;">​</a></h3><p>滚动条保留的真相，需要分为两种场景来看：</p><h4 id="场景-a-组件内部的容器滚动-如-div-class-scroll-box" tabindex="-1">场景 A：组件内部的容器滚动（如 <code>&lt;div class=&quot;scroll-box&quot;&gt;</code>） <a class="header-anchor" href="#场景-a-组件内部的容器滚动-如-div-class-scroll-box" aria-label="Permalink to &quot;场景 A：组件内部的容器滚动（如 \`&lt;div class=&quot;scroll-box&quot;&gt;\`）&quot;">​</a></h4><ul><li><strong>原理</strong>：滚动条的当前位置，本质上是该 DOM 节点的 <code>scrollTop</code> 和 <code>scrollLeft</code> <strong>属性值</strong>。</li><li><strong>过程</strong>：因为整个 DOM 节点被完整保存在内存中，它的 <code>scrollTop</code> 属性并没有被重置。当该 DOM 被重新 <code>appendChild</code> 挂载回页面时，浏览器渲染它时会直接读取原本的 <code>scrollTop</code>，因此滚动位置丝毫不变。</li></ul><h4 id="场景-b-最外层浏览器窗口滚动-window-document-body" tabindex="-1">场景 B：最外层浏览器窗口滚动（<code>window</code> / <code>document.body</code>） <a class="header-anchor" href="#场景-b-最外层浏览器窗口滚动-window-document-body" aria-label="Permalink to &quot;场景 B：最外层浏览器窗口滚动（\`window\` / \`document.body\`）&quot;">​</a></h4><ul><li><strong>原理</strong>：当组件 DOM 从 <code>body</code> 拔下时，页面变矮，<code>window</code> 滚动条通常会弹回顶部。</li><li><strong>解决</strong>：Vue Router 或 KeepAlive 在底层做了防丢处理：</li></ul><ol><li><strong>离开前（deactivated）</strong>：通过 <code>window.scrollY</code> 记录下当前的滚动像素值（例如 <code>350px</code>）。</li><li><strong>切回后（activated）</strong>：在 DOM 重新插入后，自动调用一次 <code>window.scrollTo(0, 350)</code> 恢复视角。</li></ol><h2 id="四、-踩坑细节与解决方案" tabindex="-1">四、 踩坑细节与解决方案 <a class="header-anchor" href="#四、-踩坑细节与解决方案" aria-label="Permalink to &quot;四、 踩坑细节与解决方案&quot;">​</a></h2><h3 id="_1-组件命名陷阱-include-exclude-无效" tabindex="-1">1. 组件命名陷阱（<code>include</code> / <code>exclude</code> 无效） <a class="header-anchor" href="#_1-组件命名陷阱-include-exclude-无效" aria-label="Permalink to &quot;1. 组件命名陷阱（\`include\` / \`exclude\` 无效）&quot;">​</a></h3><p>当使用 <code>include</code> 或 <code>exclude</code> 筛选缓存组件时，Vue 是根据<strong>组件的 <code>name</code> 选项</strong>进行匹配的，而不是路由的 <code>name</code>！</p><ul><li><strong>避坑做法</strong>：确保被缓存的组件显式定义了 <code>name</code> 属性。</li></ul><div class="language-vue vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">&lt;!-- 错误：没有 defineOptions 指定名字，或者文件名与 include 匹配不上 --&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">script</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Vue 3.3+ 推荐写法</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineOptions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;UserProfile&#39;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 👈 这里的名字必须与 include=&quot;UserProfile&quot; 一致！</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">script</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><h3 id="_2-定时器与全局事件导致的-内存泄漏" tabindex="-1">2. 定时器与全局事件导致的“内存泄漏” <a class="header-anchor" href="#_2-定时器与全局事件导致的-内存泄漏" aria-label="Permalink to &quot;2. 定时器与全局事件导致的“内存泄漏”&quot;">​</a></h3><p>这是 <code>&lt;KeepAlive&gt;</code> 最常见的问题！组件失活（<code>deactivated</code>）时，<strong>它并没有被销毁</strong>，如果在 <code>onMounted</code> 里注册了事件或定时器，它们会<strong>一直在后台运行</strong>，极易引发内存泄漏或逻辑异常。</p><ul><li><strong>错误现象</strong>：在 Tab A 挂载了一个 <code>setInterval</code> 轮询，切到 Tab B 后，后台还在频繁发请求。</li><li><strong>避坑做法</strong>：把监听器/定时器的“创建和清除”绑定到 <code>onActivated</code> 和 <code>onDeactivated</code> 上。</li></ul><div class="language-javascript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { onActivated, onDeactivated } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;vue&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> timer </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">onActivated</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 切回前台：开启定时器 / 绑定 window 事件</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  timer </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setInterval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fetchData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() }, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  window.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addEventListener</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;resize&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, handleResize)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">onDeactivated</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 移到后台：及时暂停定时器 / 解绑事件！</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  clearInterval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(timer)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  window.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">removeEventListener</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;resize&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, handleResize)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br></div></div><h3 id="_3-与-transition-过渡动画结合时的嵌套顺序" tabindex="-1">3. 与 <code>&lt;Transition&gt;</code> 过渡动画结合时的嵌套顺序 <a class="header-anchor" href="#_3-与-transition-过渡动画结合时的嵌套顺序" aria-label="Permalink to &quot;3. 与 \`&lt;Transition&gt;\` 过渡动画结合时的嵌套顺序&quot;">​</a></h3><p>如果你在路由切换时同时使用了 <code>&lt;router-view&gt;</code>、<code>&lt;KeepAlive&gt;</code> 和 <code>&lt;Transition&gt;</code> 动画，<strong>结构顺序绝对不能错</strong>，否则会导致动画失效或 DOM 渲染异常！</p><ul><li><strong>正确结构（Vue 3 标准嵌套）</strong>：</li></ul><div class="language-vue vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">router-view</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> v-slot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ Component }</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;transition name=&quot;fade&quot;&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;!-- KeepAlive 必须在 transition 内部，包装具体的 component --&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;keep-alive :max=&quot;10&quot;&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      &lt;component :is=&quot;Component&quot; /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;/keep-alive&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;/transition&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">router-view</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><h3 id="_4-数据更新策略-从-什么时候调接口-说起" tabindex="-1">4. 数据更新策略：从“什么时候调接口”说起 <a class="header-anchor" href="#_4-数据更新策略-从-什么时候调接口-说起" aria-label="Permalink to &quot;4. 数据更新策略：从“什么时候调接口”说起&quot;">​</a></h3><p>使用了 <code>&lt;KeepAlive&gt;</code> 的组件，第二次进入时**再也不会触发 <code>onMounted**</code>。如果你希望用户每次切回该页面时数据都是最新的，调接口的时机必须做出调整：</p><ul><li>❌ <strong>不推荐</strong>：把数据加载仅放在 <code>onMounted</code>（切回时不更新）。</li><li>✅ <strong>推荐做法</strong>：</li><li><strong>场景 A</strong>：只有需要实时刷新的数据（如最新通知），放在 <code>onActivated</code> 中请求。</li><li><strong>场景 B</strong>：通过路由参数监听（如 <code>watch(() =&gt; route.params.id)</code>）判断参数是否改变，变了才重新发请求，没变就继续复用缓存。</li></ul><h3 id="_5-性能陷阱-什么时候绝对不要用-keepalive" tabindex="-1">5. 性能陷阱：什么时候<strong>绝对不要</strong>用 KeepAlive？ <a class="header-anchor" href="#_5-性能陷阱-什么时候绝对不要用-keepalive" aria-label="Permalink to &quot;5. 性能陷阱：什么时候**绝对不要**用 KeepAlive？&quot;">​</a></h3><p><code>&lt;KeepAlive&gt;</code> 并非万能药，盲目添加会严重拖慢性能：</p><ol><li><strong>含有大量 DOM 节点或超长列表的组件</strong>：</li></ol><ul><li>缓存 10 个包含 2000 行表格数据的组件，相当于把几万个真实 DOM 节点死死锁在 RAM 内存里，会导致整张网页卡顿。</li></ul><ol start="2"><li><strong>安全敏感页面</strong>：</li></ol><ul><li>如支付界面、个人隐私设置、修改密码页面。缓存会导致用户切出后，表单敏感数据依然停留在内存中，有安全风险。</li></ul><ol start="3"><li><strong>没有频繁切换需求的单次操作页</strong>：</li></ol><ul><li>如“新增用户”表单页，用户填完提交就走，不需要保留状态，直接使用常规组件卸载流程即可。</li></ul><h2 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结&quot;">​</a></h2><p><code>&lt;KeepAlive&gt;</code> 的设计精妙之处在于对<strong>底层 DOM 引用</strong>和<strong>生命周期拦截</strong>的精准把控：</p><ul><li><strong>存得巧</strong>：利用 JS 引用机制将真实 DOM 驻留内存，规避 GC 垃圾回收。</li><li><strong>查得快</strong>：跳过 JS 逻辑与 DOM 创建，将渲染开销降至最低。</li><li><strong>细节全</strong>：利用 DOM 原生属性或主动恢复机制，完美保留滚动位置与用户输入状态。</li></ul>`,80))])}const y=n(d,[["render",h]]);export{v as __pageData,y as default};
