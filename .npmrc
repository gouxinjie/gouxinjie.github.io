# pnpm 配置文件
# 使用严格的对等依赖
strict-peer-dependencies=false

# 使用 shamefully-hoist 提升依赖（解决某些包的兼容性问题）
shamefully-hoist=true

# 自动安装对等依赖
auto-install-peers=true

# 使用国内镜像加速（可选，根据需要启用）
# registry=https://registry.npmmirror.com
